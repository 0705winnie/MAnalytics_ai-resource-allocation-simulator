# Archive

Documents here describe an earlier state of the repository and are kept for
history rather than as current reference. Each archived file retains its
original content with a short notice at the top explaining what changed and
where to find the current equivalent.

| File | Why it's archived |
|---|---|
| `project-structure-audit.md` | Audited the codebase at commit `0b7c0f4`, before the account/course/authentication system existed. Its statements that there is "no active database layer" and "no authentication or authorization implementation" are no longer true — see the root `README.md` (Architecture, Setup, Instructor and student workflow) for the current structure. |
